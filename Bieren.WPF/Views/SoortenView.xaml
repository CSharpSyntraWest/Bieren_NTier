<UserControl x:Class="Bieren.WPF.Views.SoortenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bieren.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <DataGrid IsReadOnly="True" ItemsSource="{Binding Soorten}" SelectedItem="{Binding SelectedSoort}" Height="200" EnableRowVirtualization="True"  SelectionChanged="DataGrid_SelectionChanged"/>
        <local:SoortDetailsView DataContext="{Binding}"/>
    </StackPanel>
</UserControl>
